#include<stdio.h>
int main()
{
    int c,d=0;
    long long int a[10];
    printf("enter 10 numbers");
    for(int i=0;i<=0;i++)
    {
        scanf("%lld",&a[i]);
    }
    printf("enter the numbbbbbbe searched");
    scanf("%d",&a);
    for(int i=0;i<10;i++)
    {
        if (a[i]== c)
        d++;
    }
    printf("the number of occourance of %d id %d",a,d);
return 0;
}